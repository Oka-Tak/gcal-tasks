# Kairos コード監査レポート

- 監査日: 2026-07-14
- 対象: `/home/zundamon/gcal-tasks`
- ブランチ: `nextjs-rewrite`
- 監査方法: 読み取りベースのコードレビュー、静的検査、ビルド、Drizzle スキーマ検査、依存脆弱性検査
- 初回監査: アプリケーションコードを変更せず問題を抽出
- 修正状況: 2026-07-14に、利用者が承認した項目を最小構成で実装（Phase一覧を参照）

## 1. 結論

Kairos は TypeScript のビルドと Drizzle のスキーマ検査を通過しており、認証、Google API、SQLite、AI エージェント、通知、音声ノートなどの主要機能も一通り実装されている。

一方、リファクタリングを始める前に修正すべきデータ整合性・セキュリティ上の問題がある。特に次の5項目を最優先とする。

1. Google Tasks 同期にページネーションを実装する。
2. AI 子プロセスへ渡す環境変数を、CLIに必要な値を残した許可リストへ変更する。
3. 提案承認を原子的かつ冪等にする。
4. Docker ビルドへ秘密情報・DB・アップロードデータを持ち込まない。
5. DBマイグレーション、入力検証、アップロード制限をfail-safeにする。

上記のうち、Google同期、子プロセス環境変数、並行承認、`.dockerignore`、DBマイグレーション、入力検証、アップロード総量制限は2026-07-14の修正で対応済み。Next.jsの広すぎるファイルトレース警告は残課題である。

## 2. 監査範囲

次の領域を確認した。

- `app/`: 画面、Route Handlers、フォーム、アップロード処理
- `lib/`: Google 同期、DB、AI、通知、ノート、ログ、経費、プランナー
- `drizzle/`: スキーマとマイグレーション
- `scripts/`: インポート、エクスポート、通知、移行スクリプト
- `auth.ts`、`proxy.ts`: 認証・アクセス制御
- `next.config.ts`、`Dockerfile`: ビルド・デプロイ・セキュリティヘッダー
- `package.json`、`package-lock.json`: 依存関係と実行スクリプト
- `README.md`、`CONTEXT.md`、`DEPLOY.md`: 実装と設計方針の整合性

ソースコード量は TypeScript/TSX等で約14,000行。主要な大規模ファイルは次の通り。

| ファイル | 規模 | 主な責務 |
| --- | ---: | --- |
| `app/calendar.tsx` | 約1,376行 | カレンダー、タスク、同期、詳細表示 |
| `app/notes/notes-client.tsx` | 約797行 | ノート一覧、編集、アップロード |
| `lib/chat.ts` | 約572行 | 会話、RAG、プロンプト構築 |
| `lib/agent.ts` | 約547行 | Claude/Codex等のCLI実行 |
| `app/logs/logs-client.tsx` | 約546行 | ログUI |
| `lib/notes.ts` | 約537行 | 音声処理、要約、RAG連携 |
| `lib/actions.ts` | 約514行 | AI提案の検証と実行 |

## 3. 検証結果

| 検証 | 結果 | 補足 |
| --- | --- | --- |
| `npm run build` | 成功 | ワークスペースルートとファイルトレースの警告あり |
| `npm run lint` | 成功 | 0エラー、0警告 |
| `npx tsc --noEmit` | 成功 | 型エラーなし |
| `npx drizzle-kit check` | 成功 | スキーマ定義に不整合なし |
| `npm audit --omit=dev` | 中3件 | 高0件、重大0件。実体は PostCSS 由来の1系統 |
| `npm test` | 成功 | 17テスト |
| スクリーンショットE2E | 成功 | 認証済み月表示・週表示を確認 |

初回監査で検出したLint問題はすべて解消済み:

- render 中の `Date.now()` 使用
- effect 内での同期的な state 更新
- React Hook の依存配列不足
- 手動メモ化と推論依存関係の不一致
- import スクリプトの明示的 `any`

依存脆弱性については `postcss@8.4.31` の XSS アドバイザリが `next` と `next-auth` に伝播している。`npm audit fix` が提示する Next.js 9へのダウングレードは適用せず、互換性を確認した上で Next.js の更新または安全な PostCSS 解決方法を選ぶ。

## 4. P0: 最優先で修正する問題

### P0-1. 100件を超えるGoogle Tasksが削除扱いになる

対象:

- `lib/sync.ts:162` `syncTasklists()`
- `lib/sync.ts:197` `syncTasks()`

監査時点では `maxResults: 100` で1ページだけ取得し、`nextPageToken` を処理していなかった。その後、今回取得されなかった既存行を `deletedAt` 付きに更新していた。

そのため、101件目以降の正常なタスクやタスクリストがローカルミラー上で削除扱いになる。ローカル専用の時刻、優先度、リマインダー等も画面から見えなくなる。

対応:

- `nextPageToken` がなくなるまで全ページを取得する。
- 全ページ取得に成功した場合だけ未取得行を削除扱いにする。
- 途中失敗時は削除フェーズを実行しない。
- カレンダー一覧同期にも同じページネーション方針を適用する。

受け入れ条件:

- 150件以上のタスクを同期しても全件残る。
- 2ページ目の取得失敗時に既存タスクが削除扱いにならない。
- 本当にGoogle側で削除されたタスクだけがソフト削除される。

### P0-2. AI自動化の直接書き込み（修正対象外・意図的設計）

対象:

- `lib/task-enrich.ts:138`
- `app/api/money/route.ts`

タスクのAI推定、サブタスク自動分割、毎日0時の自動実行は、利用者が明示的に無承認での実行を求めた機能である。経費の画像取り込みも「撮ったら記録される」クイックキャプチャとして直接保存する。

提案フローはチャットからの対話的変更に使い、バックグラウンド自動化とクイックキャプチャは直接書き込む二本立てを維持する。

既存のガードレール:

- 手動値は上書きせず、空欄だけを推定で埋める。
- 自動分割の件数に上限がある。
- 二重実行ガードと分割済みマークがある。
- 生成されたサブタスクは通常のタスクとして削除できる。

禁止事項:

- バックグラウンド自動化へ承認フローを追加しない。
- 経費画像取り込みを下書き方式へ変更しない。
- チャット用の提案フローを全AI処理へ一律適用しない。

### P0-3. AI子プロセスがアプリの秘密情報を継承する

対象:

- `lib/agent.ts:54` `spawnCapture()`
- `lib/agent.ts:142` `spawnClaudeStream()`
- `lib/agent.ts:212` `spawnCodexStream()`
- `lib/agent.ts:316` `runCodex()`

現在は `{ ...process.env }` を子プロセスへ渡すため、Google OAuth、Auth.js、暗号化キー、通知トークン等がエージェントから参照できる。またCodexではWeb検索が有効になっている。

カレンダー、ノート、RAG、チャット添付などの信頼できないテキストがプロンプトへ入るため、プロンプトインジェクションと秘密情報漏えいを組み合わせられる余地がある。

対応:

- 子プロセス用環境変数を許可リスト方式にする。
- アプリの秘密情報を子プロセスへ一切渡さない。
- `HOME`、`PATH`、`CLAUDE_CONFIG_DIR`、各CLIの認証ディレクトリは維持する。
- `claude-pool` が動的に渡すアカウント切替用の設定を維持する。
- CodexのWeb検索を無効化しない。
- stdout/stderrの最大サイズを設定する。

別OSユーザーや別コンテナへの分離は、個人所有の単一ホスト構成では行わない。

受け入れ条件:

- エージェントからOAuthトークンやアプリ秘密鍵を参照できない。
- 出力が上限を超えた場合、安全に打ち切られる。
- Claude/Codexとアカウント切替が従来どおり動作する。

### P0-4. 提案承認が二重実行され得る

対象:

- `lib/actions.ts:477` `decideProposal()`

現在は `pending` 状態を読み、外部APIを実行した後で `done` を保存する。同じ提案に対する2リクエストが並行すると、両方が `pending` を読み、同じ予定やタスクを二重作成する可能性がある。

対応:

- 条件付きUPDATEで `pending -> running` を原子的に確保する。
- 確保できた1リクエストだけが外部APIを実行する。
- 外部API操作に提案ID由来の冪等性キーまたは重複検出を導入する。
- `running` のまま停止した提案を回収できるようにする。

受け入れ条件:

- 同一提案を並行承認しても副作用が1回だけ発生する。
- クライアント再送時に結果を安全に再取得できる。
- プロセス停止後に `running` 状態を復旧できる。

### P0-5. Dockerビルドに個人データと秘密情報が入る

対象:

- `Dockerfile:13` `COPY . .`
- `.dockerignore` が存在しない
- `Dockerfile:24` 存在しない `public` のコピー

現在のビルドコンテキストには `.env.local`、SQLite DB、音声、アップロード、Git情報、ローカルデータが含まれる。これらがビルドステージやキャッシュへ入る可能性がある。

Next.jsのファイルトレースも、DB、音声、アップロードをランタイム依存として列挙し、「プロジェクト全体を意図せずトレースしている」と警告している。

対応:

- `.dockerignore` を追加する。
- `.env*`、`*.db*`、`data/`、`uploads/`、`.git/`、`.next/`、`node_modules/` を除外する。
- ランタイムデータは明示したボリューム配下だけに保存する。
- `outputFileTracingRoot` / Turbopack rootと動的パス利用を見直す。
- `public` がない構成でもDockerビルドできるようにする。

本番はTailscale Serve + systemdであり、Dockerは任意の開発・補助経路に限る。AI・Whisper機能のコンテナ対応など、Dockerを前提としたアーキテクチャ変更は行わない。

受け入れ条件:

- Dockerビルドコンテキストに秘密情報、DB、音声が含まれない。
- クリーン環境でイメージをビルドして起動できる。
- 永続データがコンテナ再作成後も指定ボリュームに残る。

## 5. P1: 正しさ・運用性の問題

### P1-1. DBマイグレーション失敗を無視する

`lib/db/index.ts:18` はマイグレーション例外をログに出すだけで、DBクライアントを返す。不完全なスキーマでアプリが動き続けるため、障害原因が分かりにくくなる。

対応は起動失敗へ変更し、バックアップ、適用前検査、ロールバック手順をDEPLOY文書へ追加すること。

### P1-2. 音声ノート処理が再起動後に復旧しない

`lib/notes.ts:31` のコメントでは、停止した処理を次回読み込み時に `error` へ移すとしているが、実装されていない。プロセス再起動後も `transcribing` や `summarizing` のまま残る。

対応:

- 既存の更新時刻を使ってstale状態を判定する。
- staleな処理を最小実装で再実行またはエラー化する。

永続ジョブキューの導入は行わない。

### P1-3. タイムゾーンが実行環境依存

通知、日次ブリーフィング、終日予定、期限判定がサーバーのローカル時刻へ依存している。Dockerには `TZ=Asia/Tokyo` がなく、UTC環境では最大9時間ずれる。

対応は実行環境で `TZ=Asia/Tokyo` を明示する範囲に留める。動作中の `dateFromTitle`、回別フォルダ判定、プランナーを巻き込む日付型の全面的な変更は行わない。

### P1-4. 入力検証がRouteごとに不統一

通常のタスク・予定APIでは、AI提案側ほど厳密な検証を行っていない。不正な日時、優先度、ステータス、親子階層、読み取り専用カレンダーへの書き込み等を共通層で拒否すべきである。

対応はZod等の共通スキーマをRouteとAI提案の両方から利用し、ドメイン検証を `lib/mutations.ts` 側でも必ず実行すること。

### P1-5. 大容量アップロードをメモリへ展開する

音声は各ファイル最大500MBだが、複数ファイルの件数・合計サイズ上限がない。`formData()` と `arrayBuffer()` でメモリへ展開するため、プロセス停止につながる。

対応:

- リクエスト総量、ファイル数、展開後サイズを制限する。
- MIMEと実ファイル形式を検証する。
- ストリーミング保存へ移行する。
- 保存途中の失敗時にファイルを削除する。

### P1-6. 論理削除後も関連データが残る

`lib/notes.ts:128` はノート行をソフト削除し、エクスポートファイルだけを削除する。音声、`noteAudios`、Open WebUI/RAG上のファイルは残るため、削除済み情報が検索結果へ出る可能性がある。

ログ画像、経費画像、チャット添付にも統一されたライフサイクル管理がない。

対応はノート削除時に音声とOpen WebUI上のファイルを掃除する最小実装とする。保持期間ポリシー、定期削除ジョブ、管理画面は作らない。

### P1-7. 通知が重複・多重実行され得る

通知HTTPにタイムアウトがなく、定期処理にもin-flightガードやプロセス間ロックがない。前回処理が終わる前に次のtickが始まったり、複数ワーカーから同じ通知を送ったりする可能性がある。

対応はntfyへのHTTPタイムアウト追加に留める。単一プロセス運用のため、DB送信claimやプロセス間ロックは導入しない。

### P1-8. 削除済みイベントがブリーフィングに混入する

`lib/briefing.ts:31` のイベント取得条件に `deletedAt IS NULL` がない。Google側で消えた予定が日次通知へ出る可能性がある。

## 6. P2: 性能・保守性の改善

### P2-1. Google同期をリクエスト処理から分離する

ページ表示のたびに複数アカウント・カレンダー・タスクリストを同期するため、Google APIの応答待ちが画面表示時間へ直結する。

改善案:

- 画面はローカルDBを即時返却する。
- 同期はバックグラウンドワーカーで実行する。
- Calendar APIの `syncToken` を利用する。
- 同期状態、最終成功時刻、エラーをUIに表示する。

### P2-2. ローカル専用変更でGoogle再同期しない

タスクの時刻、優先度、見積時間、Kanban列などのローカル専用変更でもGoogle同期を行っている。待ち時間、障害範囲、APIクォータ消費を増やす。

Google所有フィールドとローカル所有フィールドをコマンドレベルで分離し、ローカル変更はSQLiteトランザクションだけで完了させる。

### P2-3. フロントエンドの古いレスポンスによる上書き

表示期間やビューを素早く切り替えた場合、古いHTTPリクエストが後から完了し、新しい状態を上書きする可能性がある。

`AbortController`、request ID、データ取得ライブラリのいずれかで最新リクエストだけを採用する。

### P2-4. 複数日にまたがるイベントの表示

時刻指定イベントは開始日だけに表示される。夜をまたぐ予定や複数日イベントを日ごとのセグメントへ分割して描画する必要がある。

### P2-5. チャット履歴とプロンプトが無制限に増える

スレッドの全履歴、カレンダー、タスク、ログ、ノート、RAGを毎回プロンプトへ入れ、再構築済みプロンプトもDBへ保存している。利用期間に比例してコスト、遅延、DB容量が増える。

改善案:

- 直近メッセージのスライディングウィンドウ
- 古い会話の要約
- RAG取得件数と文字数上限
- プロンプトと取得コンテキストにサイズ上限を設ける

`agent_jobs` へのプロンプト全文保存はAIの挙動を後から追うための意図的なデバッグ機能なので維持する。参照IDだけを保存する方式には変更しない。

### P2-6. 巨大コンポーネントと責務集中

`app/calendar.tsx` は表示、通信、キャッシュ、編集、ドラッグ操作を1ファイルで扱っている。ノートとログ画面にも同じ傾向がある。

このファイルはClaude側からも頻繁に変更されるため、分割はPhase 3の最後に行う。大規模な一括変更は禁止し、小さいPRごとにスクリーンショットE2Eで表示を確認する。

推奨分割:

- データ取得hooks
- 表示期間・タイムゾーン計算
- カレンダーグリッド
- イベント編集フォーム
- タスクボード
- キャッシュと同期状態

### P2-7. 複合キー不足

親子タスクのマップをGoogle IDだけで構築する箇所がある。複数アカウント・タスクリストでは、`account + tasklist + googleId` を一貫した識別子として使う。

### P2-8. 環境依存パス

一部スクリプトが `/home/zundamon`、`~/gcal-tasks` 等をハードコードし、`DEPLOY.md` の `/opt/kairos` と一致しない。リポジトリルート、データディレクトリ、状態ファイルを環境変数またはCLI引数に統一する。

## 7. セキュリティ設定上の改善と非対象

- 本番はCloudflareではなくTailscale Serveでtailnet内だけに公開している。`proxy.ts` のCloudflare関連処理は過去の名残であり、Cloudflare設定の有無によるフェイルクローズ化は行わない。
- ウィジェットのクエリトークンはiPhoneのScriptable/KWGT互換性のため維持する。`Authorization` ヘッダーは追加で受け付けてもよいが、クエリ方式を廃止しない。
- トークン比較に定時間比較を使う。
- `FRAME_ANCESTORS` の値を検証してからCSPへ挿入する。
- CSPの `unsafe-inline` をnonce/hash方式へ段階的に移行する。
- 512MBのプロキシボディ上限を全Routeへ適用せず、アップロード経路を分離する。
- localStorageの7日キャッシュは起動直後のstale-while-revalidate表示に必要なので維持する。

## 8. テスト戦略

初回監査時は自動テストがなかった。今回、ページネーション、日またぎ表示、複合キー、入力・アップロード検証、
秘密値比較、子プロセス環境変数等の17テストを追加した。以下のうち未着手の統合・E2E範囲は今後も拡充する。

### 単体テスト

- Google APIページネーション
- 同期途中失敗時のソフト削除抑止
- 日付、タイムゾーン、終日予定変換
- 提案payloadの検証
- タスク親子階層と複合キー
- プランナーのソート・時間枠計算

### DB・統合テスト

- SQLiteマイグレーション
- ローカル専用フィールドの同期後保持
- 提案の並行承認
- ノートジョブのstale回収
- 論理削除と関連ファイル削除

### E2Eテスト

- ログイン後の予定・タスク表示
- タスク作成、編集、完了
- AI提案の承認・拒否
- 音声ノートの投入から完了まで
- 同期失敗時のエラー表示と再試行
- `calendar.tsx` の変更時はスクリーンショット比較で主要ビューを確認

## 9. 推奨リファクタリング順序

### Phase 0: 安全性とデータ保護

- [x] Tasks/Tasklists/CalendarListのページネーション
- [x] 同期途中失敗時の削除抑止
- [x] AI/Whisper/CLI子プロセスの環境変数制限（認証ディレクトリとWeb検索は維持）
- [x] 提案承認の原子的claimによる並行二重実行防止
- [ ] 外部API実行中のプロセス停止に対する冪等性・回収
- [x] `.dockerignore` 追加
- [ ] Next.jsの広すぎるファイルトレース修正
- [x] DBマイグレーションのfail-fast化

### Phase 1: 正しさと運用性

- [x] タスク・予定の共通入力スキーマと書き込み先検証
- [x] 経費・生活ルールの入力検証
- [x] `TZ=Asia/Tokyo` の明示（全面的な日付型変更はしない）
- [x] ノート処理のstale回収（永続キューは作らない）
- [x] ntfyのHTTPタイムアウト
- [x] ノート削除時の音声・OWUI掃除
- [x] アップロードのファイル数・個別・総量制限
- [ ] 大容量アップロードのストリーム化
- [x] ブリーフィングから論理削除済み予定を除外

### Phase 2: 性能

- [ ] バックグラウンド差分同期
- [ ] ローカル専用更新とGoogle同期の分離
- [ ] チャット履歴の要約・上限
- [x] フロントエンドで古いレスポンスを破棄
- [x] 日またぎ時刻指定イベントを各日に表示
- [x] タスク親子関係をアカウント・リスト込みの複合キーへ変更
- [ ] DBインデックスとクエリ計測

### Phase 3: 構造改善

- [ ] `calendar.tsx` の段階的な責務分割（小さいPR + スクリーンショットE2E）
- [ ] Route Handlerとドメインサービスの分離
- [ ] ファイル保存・RAG・DB更新のトランザクション設計
- [ ] 環境依存パスの設定化
- [ ] 単体・統合・E2EテストのCI化

### 横断的な完了項目

- [x] Lint 6エラー・10警告を解消
- [x] Node Test Runner + tsxによる回帰テストを追加
- [x] ウィジェット・学務取込トークンを定時間比較へ変更（クエリ方式は維持）
- [x] 月表示・週表示の再利用可能なスクリーンショットE2Eスクリプトを追加

## 10. 良好だった点

修正が必要な箇所だけでなく、維持すべき設計もある。

- TypeScriptビルドが成功している。
- Drizzleのスキーマ定義は検査を通過している。
- Google OAuthトークンはAES-256-GCMで暗号化保存されている。
- エージェントのコマンド起動はシェル文字列連結ではなく引数配列を使っている。
- AI提案には実行前の再検証がある。
- Googleイベント同期にはページネーションが実装されている。
- Google所有フィールドのupsert時にローカル専用フィールドを上書きしない。
- Route Handlerの多くでセッション確認を行っている。

これらを回帰テストで固定した上で、上記Phase 0から順に進める。

## 11. 対応方針の確定（レビュー: Claude, 2026-07-14）

修正済み: P0-1(ページネーション+途中失敗時の削除抑止), P0-3(agent-env許可リスト方式・認証ディレクトリは残置), P0-4(提案承認のpending→running原子化), P0-5(.dockerignore+TZ), P1-1(migration fail-fast), P1-2(staleノート回収), P1-4(write-validation共通化+対象存在・書込権限検査), P1-5(アップロード総量制限), P1-6(ノート削除時のaudio/OWUI掃除), P1-8(briefing deletedAt), P2-3, P2-4(日またぎセグメント), P2-7(複合キー task-identity), lint全解消, 単体テスト17件。

**意図的に修正しない項目**（Codexは今後も変更しないこと）:

- **P0-2**: バックグラウンドAI（task-enrich の見積り/優先度/サブタスク分割、経費画像取り込み）の直接書き込みは仕様。ユーザーが「0時に勝手に」を明示要求している。提案フローは対話チャット用との二本立て。ガードは空欄のみ埋め・分割予算・二重実行ガードで担保。
- **セキュリティ章「Cloudflareフェイルクローズ」**: 本番はTailscale閉域でCloudflare不使用。フェイルクローズにすると締め出される。
- **セキュリティ章「ウィジェットトークンのヘッダー化」**: クエリ方式はiPhoneウィジェットの制約由来で維持（定時間比較のみ導入済み）。
- **localStorageキャッシュ保持期間**: 7日は起動即描画(stale-while-revalidate)のための仕様。
- **P2-5のうち「プロンプト全文をDB保存しない」**: agent_jobsの全文保存はデバッグ用途で意図的（上限カットは可）。
- **P0-3のうち「別OSユーザー/コンテナ分離」・P1-2「永続ジョブキュー」・P1-6「保持期間ポリシー+削除ジョブ」**: 個人1台構成にはオーバーエンジニアリングのため見送り。
- **CodexのWeb検索有効化**: 意図的な設定。
